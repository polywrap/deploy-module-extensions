#import { Module } into Deploy from "wrap://ens/wrappers.polywrap.eth:deploy-module-ext@1.0.0"
#import { Module } into ENS from "wrap://ens/wrappers.polywrap.eth:ens@1.0.0"

type Module implements Deploy_Module {}

type Env {
  domainName: String!
  ensRegistryAddress: String!
}
